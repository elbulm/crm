# Интеграм: единый интерфейс «Контур»

[Главная страница и все примеры](https://elbulm.github.io/crm/redesign/) · [Общий реестр с обучением](https://elbulm.github.io/crm/redesign/?data=stress&scenario=registry#contour) · [История изменений](https://elbulm.github.io/crm/redesign/changes.html)

## Главная и пошаговые примеры · 7 сентября 2026

Из трёх вариантов оставлен «Контур»: спокойная навигация, умеренная плотность, таблица по умолчанию и список текущей выборки рядом с карточкой. Компактные строки и доска остаются обычными режимами работы. Старые адреса `#register` и `#focus` открывают единый интерфейс; их прежние локальные данные не удаляются.

Главная ведёт в общий реестр и шесть подготовленных сценариев:

- `scenario=groups`: контрагент → этап → месяц → исходные договоры → возврат к группировке.
- `scenario=columns`: 36 полей → свёрнутая секция → выбор опорного показателя.
- `scenario=pivot`: направление и состояние платежей × год и квартал → исходные платежи → возврат в сводную.
- `scenario=hierarchy`: родительская задача → подзадача → поиск с сохранением контекста.
- `scenario=relations`: договор → связанные позиции → полный состав → карточка → возврат.
- `scenario=dictionaries`: источник значений → использующие значение договоры → карточка → справочник.

В каждом входе обучение подсвечивает настоящий элемент и ждёт нужного действия и результата. Доступны пропуск, Escape, повторный запуск и восстановление после перехода в другой раздел. При перезагрузке пропущенное или завершённое обучение не запускается повторно. Темы, клавиатура и мобильные экраны поддерживаются.

Примеры сохраняют настройки видов в отдельных ключах браузера и не перезаписывают текущий вид обычного реестра. Все сценарии обучения читают записи без изменения данных. Ручное редактирование остаётся доступно; база и справочники общие для локального демонстрационного набора.

Новые проверки: `npm run onboarding` в `tests/redesign`. Они проходят каждый сценарий по подсвеченным элементам и проверяют получившиеся записи, контекст, сохранение раскрытия, пропуск, старые ссылки и доступность.

## Сложные группировки · 6 сентября 2026

Все четыре механизма работают в едином интерфейсе на локальных демонстрационных данных:

- [Вложенные группы строк](./?data=stress&example=groups#contour): до шести уровней, независимое раскрытие, поиск групп, сортировка групп отдельно от записей, условия на итоги, пустые значения справочников и полный CSV отчёта. Для дат доступны год, квартал, месяц и ISO-неделя; для чисел — диапазоны; для меток — сочетания или участие в каждой группе. По связанным полям можно пройти до двух переходов.
- [Секции столбцов](./?data=stress&screen=table#contour): готовые и собственные секции, вложенность, порядок, закрепление целой секции, одно или несколько опорных полей при сворачивании. Скрытые фильтры и сортировка обозначены на заголовке. Порядок и состав секций не меняют структуру таблицы.
- [Иерархия задач](./?data=stress&table=tasks&screen=hierarchy#contour): 6 000 задач с родительскими ссылками, раскрытие, поиск с сохранением пути, независимые страницы веток. Циклы и отсутствующие родители обозначаются. [Связанные таблицы договоров](./?data=stress&table=deals&screen=hierarchy#contour) раскрываются со своими полями и переходом к полному редактируемому реестру.
- [Сводная матрица](./?data=stress&screen=pivot#contour): вложенные оси строк и столбцов, независимое раскрытие, промежуточные и общие итоги, несколько показателей, детализация ячейки до записей и возврат. CSV сводной и CSV исходных записей доступны отдельно.

Количество, уникальные значения, сумма, среднее, минимум и максимум считаются по полному отфильтрованному набору. Валюты и единицы измерения разделяются; общий итог пересекающихся групп меток не дублирует записи. Суммирование чисел из связанных таблиц не предлагается, чтобы не умножать сумму договора на число его позиций.

Перенос между группами показывает изменяемые поля и число записей. Для периода, диапазона или метки требуется конкретное подходящее значение; перемещение можно отменить. Переход к справочнику сохраняет контекст. Срез записей, открытый из группы или сводной, можно сохранить как фиксированный список ID; удалённые записи не заменяются другими. Слишком длинная ссылка на такой вид отклоняется с объяснением.

На экран выводится ограниченная порция, а итоги охватывают весь набор. Очень широкая матрица или чрезмерное число сочетаний дают явное сообщение; усечённый результат не выдаётся за полный. В этой версии все исходные данные уже находятся в браузере. Серверная загрузка рабочих данных и права производственного приложения сюда не подключены.

**Проверки новых механизмов:** 70 модельных сценариев и 16 браузерных цепочек единого интерфейса, включая 50 тысяч строк, сохранение, исправления пограничных ошибок, темы, клавиатуру и мобильные экраны. Дополнительно проверяется прежний функционал; воспроизводимые команды находятся в `tests/redesign/README.md`.

## Большие таблицы и крайние случаи · 6 сентября 2026

[Открыть набор «Рабочие крайности»](https://elbulm.github.io/crm/redesign/?data=stress#contour). Он доступен во всех трёх вариантах через переключатель «Данные»; на телефоне переключатель находится в «Настройках просмотра».

| Таблица | Строк | Полей |
|---|---:|---:|
| Договоры и поставки | 12 000 | 36 |
| Позиции поставок | 50 000 | 18 |
| Платежи и сверка | 25 000 | 22 |
| Задачи и замечания | 6 000 | 17 |
| Контрагенты | 1 200 | 14 |
| Номенклатура | 1 500 | 12 |
| Команда и подрядчики | 64 | 6 |
| Склады и площадки | 18 | 7 |
| Претензии | 0 | 7 |

Всего **95 782 записи**. Это детерминированные вымышленные данные. Строки действительно существуют в локальном наборе; поиск, фильтры, сортировка и CSV работают по всему объёму. На одной странице отображаются 25, 50 или 100 записей.

В наборе есть одинаковые названия у разных записей, пропущенные номера и ответственные, записи без этапа, пустые даты, нулевые и отрицательные суммы, копейки и суммы почти в миллиард рублей, дробные количества, длинные названия и комментарии, идентификаторы без пробелов, ведущие нули, спецсимволы и текст, начинающийся со знака равенства. Справочники связаны действительными идентификаторами внутри демо; одинаковые названия дополнены кодами.

Рядом с вкладками появился **быстрый выбор вида**: все поля, просрочки, отсутствие ответственного, возвраты, исключения, частичные поставки и неразнесённые платежи. В большом справочнике фильтра есть поиск по названию или коду; уже выбранные значения сохраняются при смене поискового запроса. Длинный текст раскрывается через «⋯» в ячейке, не увеличивая каждую строку заранее.

Изменения большого набора и история отмены хранятся компактно относительно исходного демо. Редактирование одной ячейки не требует сохранять заново весь набор. Добавление и удаление поля учитываются в структуре; неизменённые строки не дублируются. Старые наборы «Пример» и «Объём» доступны отдельно.

**Проверка:** 18 сценариев на больших данных (6 цепочек × 3 варианта), включая поиск и текстовую/числовую сортировку 50 000 строк, полный CSV, фильтр по справочнику из 12 000 договоров, редактирование, отмену и повтор, удаление поля в таблице на 50 000 строк, перезагрузку, ссылки на виды и пустую таблицу. Дополнительно проверены 90 сочетаний таблиц, тем и ширин, 21 состояние доступности. В этих автоматических проверках ошибок и переполнения страницы не обнаружено. Прежние 65 сценариев также прошли.

## Проверяемые действия

Заголовок столбца циклически переключает сортировку вверх, вниз и без сортировки. Shift добавляет поле к текущей сортировке; панель «Сортировка» управляет её приоритетами. Стрелка в заголовке открывает фильтр этого поля. Фильтры поддерживают несколько условий одного поля, диапазоны, множественные значения и И/ИЛИ.

Заголовки и строки панели «Столбцы» можно перетаскивать; в панели есть стрелки изменения порядка. Основной столбец закреплён первым. Ширина меняется на границе заголовка мышью, стрелками клавиатуры после фокусировки разделителя или числом в панели. Видимость и плотность также настраиваются.

Ячейка редактируется по нажатию; Enter сохраняет, Escape отменяет. Ссылки ведут в связанные таблицы. Карточка поддерживает обязательные поля, историю фактических изменений, связанные записи и возврат к исходной выборке. «Структура» создаёт и изменяет поля. «Все таблицы» позволяет создать таблицу.

Виды сохраняют фильтры, сортировку, группы, колонки и настройки доски. Чтобы скопировать вид, сохраните его с новым именем. CSV выгружает всю выборку с видимыми столбцами; отдельные действия работают с выбранными записями. Изменения данных можно отменять и повторять в рамках открытой страницы.

На телефоне каталог и сброс находятся в «Меню» рядом с текущей таблицей; дополнительные команды таблицы — в «Ещё». Тема и набор данных доступны через «Настройки просмотра». Основные команды остаются на экране.

После повторного аудита исправлены все 12 замечаний: сохранение черновиков, обработка неверного числа, выборка доски, записи без этапа, отмена структуры вместе с видами, совместимость ссылок, ошибка хранилища с повторным сохранением, перестроение при изменении ширины, возврат фокуса, черновик фильтра, плотность строк и первый мобильный экран.

## Данные и ограничения

Это локальное демо, без подключения к рабочей CRM. Данные и виды сохраняются в localStorage отдельно для варианта и набора данных. Набор «Объём» содержит 487 записей и 24 поля. Ссылка на совместимый вид передаёт его конфигурацию, а не локальные записи и новые таблицы. Локальные изменения структуры и справочных значений могут сделать вид несовместимым со стандартным демо; создание такой ссылки блокируется с объяснением. Записи в разных браузерах не синхронизируются.

Защита от удаления проверяет ссылки на записи и таблицы. История отражает действия посетителя; начальная история не выдумывается. Старые документы использованы как описание функций; PR ideav/crm#4887 — как функциональная база для сравнения, без изменения самого PR.

## Исходники и проверки

`index.html` и `assets/` — самостоятельная версия Pages. Общие модули: seed / stress-seed (демо и большой детерминированный набор), delta (компактное хранение изменений большого набора), model (поля/запросы/экспорт), ui (таблица и настройки), views (экраны), app/actions (состояния и действия), site (переключение вариантов). Скрытые варианты не монтируются.

Проверки: [tests/redesign](../tests/redesign/README.md). Старые генераторы из локальных inline-визуализаций к этой версии не применяются: они перезапишут общий runtime прежними фрагментами.


## Справочники

[Открыть отдельный раздел](https://elbulm.github.io/crm/redesign/?data=stress&section=dictionaries#contour). В большом наборе представлены 5 таблиц-справочников и 15 списков значений. Они используют те же записи и настройки полей, что и выпадающие списки; отдельных копий данных нет. Модуль `assets/dictionaries.js` отвечает за каталог, просмотр значений и сохранение контекста переходов.
